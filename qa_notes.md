# 完整构造器交互验证

已验证当前机器人可保存到浏览器本地收藏；保存后，爱心状态切换为已保存，页面出现 `saved` 区域、单项删除入口以及 `download all` 批量 PNG 下载入口。

页面控制器已暴露完整的部件、开场动画、待机动画、色板、预设和联系表重掷入口。下一步验证导出菜单与贴纸叠层。

已验证导出菜单可见 `export png` 与 `export gif` 两项；贴纸模式可正常开启，并呈现全屏镭射贴纸、圆角滑杆、随机化、SVG 导出、关闭及「拖动倾斜」操作提示。

单页改版验证：首页不再显示独立图片工作台。`create sticker` 叠层中已出现 `upload image`、`export png`、`export svg`，并提示上传图片会生成 16px flashcard 后再进入现有镭射贴纸流程。

已用本地测试图片模拟文件选择：叠层成功切换为上传图片的像素化闪卡，控制变为 `replace image` 与 `use bot`，并保留 PNG、SVG、倾斜及关闭控件。

高保真升级验证：贴纸叠层显示默认「portrait focus → 48px flashcard」提示；上传后将提供 32/48/64px、8/12/16 色、人像聚焦与眼部细节增强控制。

已模拟上传并确认：48px 高分辨率闪卡成功接管贴纸预览，页面显示 `face focus: on`、`eye detail: on`、颜色数量及三种分辨率选项；贴纸仍可进行 PNG、SVG 导出。
